% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plots_set}
\alias{plots_set}
\title{Change a plot in a collection}
\usage{
plots_set(
  plots,
  name = NULL,
  id = NULL,
  ...,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  width = NULL,
  height = NULL,
  show = interactive()
)
}
\arguments{
\item{plots}{A collection, from \code{\link[=plots_init]{plots_init()}}.}

\item{name}{Name of the plot. The last plot in the collection is returned
when both \code{name} and \code{id} are omitted.}

\item{id}{Id of the plot, as an alternative to \code{name}. An id stays the same
when a plot is updated, so it is the safer key for code that edits a
stored collection.}

\item{...}{Changes named after the parameters the plot's customizer offers.
\code{NA} takes a value away, for example \code{title = NA} for no title.}

\item{title, subtitle, caption}{Label text, for customizers that offer them.}

\item{width, height}{Canvas size in inches. These belong to the collection
rather than the customizer, so every plot accepts them.}

\item{show}{Whether to preview the changed plot. It is shown at its true
output size in the RStudio viewer, and drawn the ordinary way anywhere
else. In a pipe of several calls each one previews, so pass \code{FALSE} when
tuning several plots at once.}
}
\value{
The collection, with that change recorded.
}
\description{
Records a change against one plot. The stored ggplot is not
touched, so a change is always reversible with \code{\link[=plots_reset]{plots_reset()}} and never
re-runs the code that built the plot.

Which changes a plot accepts depends on its customizer, and
\code{\link[=plots_params]{plots_params()}} lists them. \code{title}, \code{subtitle} and \code{caption} are named
here because most customizers offer them; anything else is passed by name
through \code{...}.
}
\examples{
library(ggplot2)
plots <- plots_init()
plots <- plots_append(plots, ggplot(mtcars, aes(wt, mpg)) + geom_point(),
                      name = "scatter", type = "point", show = FALSE)

plots <- plots_set(plots, "scatter",
                   title = "Heavier cars use more fuel",
                   caption = "Source: mtcars", width = 8, height = 5)
plots

# Take a label away again, and then forget the change entirely.
plots <- plots_set(plots, "scatter", caption = NA)
plots <- plots_reset(plots, "scatter", params = "caption")

}
