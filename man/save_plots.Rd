% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-save.R
\name{save_plots}
\alias{save_plots}
\title{Save every plot in a collection}
\usage{
save_plots(plots, dir = ".", device = "png", ...)
}
\arguments{
\item{plots}{A collection, from \code{\link[=plots_init]{plots_init()}}.}

\item{dir}{Directory to write to.}

\item{device}{File type, and the file extension. See \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{...}{Passed on to \code{\link[=save_ggplot]{save_ggplot()}}.}
}
\value{
The file paths, invisibly.
}
\description{
Writes one file per plot, each at its own canvas size. Slashes
in the plot names become folders, which are created when they are missing.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) + geom_point()

plots <- plots_init()
plots <- plots_append(plots, p, name = "Basics/scatter", width = 5, height = 4,
                      show = FALSE)

files <- save_plots(plots, dir = tempfile())
basename(files)

}
