% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plots_append}
\alias{plots_append}
\title{Add a plot to a collection}
\usage{
plots_append(
  plots,
  plot,
  name,
  type = NA_character_,
  width = NULL,
  height = NULL,
  customizer = NULL,
  ...,
  show = interactive()
)
}
\arguments{
\item{plots}{A collection, from \code{\link[=plots_init]{plots_init()}}.}

\item{plot}{A ggplot object.}

\item{name}{Name of the plot. It must be unique in the collection. Slashes
make folders when the collection is saved or browsed, for example
\code{"Module 1/awareness"}.}

\item{type}{Plot type, for example \code{"bar"} or \code{"heatmap"}. It selects the
default canvas size and groups the collection for bulk re-sizing.}

\item{width, height}{Canvas size in inches. Each falls back to a \code{\link[=canvas]{canvas()}}
already on the plot, then to the collection's default for \code{type}.}

\item{customizer}{What this plot lets people change, from \code{\link[=customizer]{customizer()}}.
The collection's own is used when this is \code{NULL}.}

\item{...}{Changes to store with the plot, named after the parameters its
customizer offers — \code{title = "…"} and so on. \code{\link[=plots_params]{plots_params()}} lists them.}

\item{show}{Whether to preview the plot. It is shown at its true output size
in the RStudio viewer, and drawn the ordinary way anywhere else.}
}
\value{
The collection, with the plot added.
}
\description{
Adds one plot to the end of a collection. The plot is stored as
it is: anything given in \code{...} is kept beside it as a change, not written
into it. Adding a name that is already in the collection updates that row
in place and keeps its \code{id} and the changes recorded against it, so
re-running a chunk is safe. A change the new plot no longer accepts is
dropped, and \code{...} replaces a change of the same name.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(factor(cyl))) + geom_bar() + labs(title = "Cylinders")

plots <- plots_init()
plots <- plots_append(plots, p, name = "Engine/cylinders", type = "bar", show = FALSE)

# The plot keeps its own title; the collection keeps the new one.
plots <- plots_append(plots, p, name = "Engine/cylinders", type = "bar",
                      title = "Most of these are eight-cylinder", show = FALSE)
plots_get(plots, "Engine/cylinders")$labels$title

}
