% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-save.R
\name{as.list.plots_tbl}
\alias{as.list.plots_tbl}
\title{Every plot in a collection, as a list}
\usage{
\method{as.list}{plots_tbl}(x, ...)
}
\arguments{
\item{x}{A collection, from \code{\link[=plots_init]{plots_init()}}.}

\item{...}{Not used.}
}
\value{
A named list of ggplot objects.
}
\description{
Returns the whole collection as a named list of plots, each one
ready to render: its labels and its canvas size are applied, exactly as
\code{\link[=plots_get]{plots_get()}} does for a single plot. The names are the plot names.

\code{lapply()} and \code{sapply()} take a list through \code{as.list()}, so on a
collection they walk its plots and not its columns.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) + geom_point()

plots <- plots_init()
plots <- plots_append(plots, p, name = "scatter", show = FALSE)
plots <- plots_append(plots, p + geom_smooth(), name = "trend", show = FALSE)

names(as.list(plots))

}
