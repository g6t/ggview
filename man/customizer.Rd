% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-customizer.R
\name{customizer}
\alias{customizer}
\title{Describe how a plot can be customized}
\usage{
customizer(apply, params)
}
\arguments{
\item{apply}{A function whose first argument is the plot. Its other
arguments are the parameters, and each one must default to \code{NULL}, meaning
"leave this alone". It returns a ggplot.}

\item{params}{A function of the plot, returning a named list of \verb{param_*()}
objects. The names are the argument names of \code{apply}.}
}
\value{
A customizer.
}
\description{
A customizer is a pair of functions. \code{apply} takes a stored
plot and the values somebody changed, and returns the changed plot.
\code{params} takes the stored plot and returns the parameters it accepts,
each one built with a \verb{param_*()} function.

Keeping the two together is what lets a program offer the right control
for each parameter without knowing anything about plots: \code{\link[=plots_params]{plots_params()}}
answers what may be changed, and \code{\link[=plots_get]{plots_get()}} applies it.

\code{\link[=customizer_default]{customizer_default()}} handles the labels every plot has. Write your own
for anything else — a palette, a set of categories to show, a text size.
A customizer may rebuild a plot from its data rather than add to it, so
what it can offer is not limited to what \code{ggplot2} lets you add on top.
}
\examples{
library(ggplot2)

# A customizer that recolors a plot, offering one color per level.
recolor <- customizer(
  apply = function(plot, colors = NULL) {
    if (is.null(colors)) return(plot)
    suppressMessages(plot + scale_fill_manual(values = unlist(colors)))
  },
  params = function(plot) {
    list(colors = param_mapping("Colors", keys = c("a", "b"), to = "color"))
  }
)
recolor

}
\seealso{
\code{\link[=customizer_default]{customizer_default()}}, \code{\link[=param_text]{param_text()}}, \code{\link[=plots_params]{plots_params()}}.
}
