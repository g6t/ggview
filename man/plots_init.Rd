% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plots_init}
\alias{plots_init}
\title{Start a plots collection}
\usage{
plots_init(
  name = NULL,
  description = NULL,
  dims = plots_dims(),
  customizer = customizer_default()
)
}
\arguments{
\item{name}{A name for the collection, for whatever shows it to people.}

\item{description}{One line saying what the collection is.}

\item{dims}{Default canvas size per plot type, from \code{\link[=plots_dims]{plots_dims()}}.}

\item{customizer}{What plots in this collection let people change, from
\code{\link[=customizer]{customizer()}}. It is the default for \code{\link[=plots_append]{plots_append()}}, which can take
another one for a single plot.}
}
\value{
An empty collection of class \code{plots_tbl}, with columns \code{id}, \code{name},
\code{type}, \code{width}, \code{height}, \code{plot}, \code{customizer} and \code{values}.
}
\description{
A plots collection keeps a set of ggplots together with the
canvas each one is drawn at and the changes somebody has made to it. It is
a tibble with one row per plot, so \code{dplyr} verbs work on it.

A change is never written into the plot. \code{\link[=plots_get]{plots_get()}} takes the stored
plot, applies the changes, then applies the canvas, so the same plot can be
changed again and again, and \code{\link[=plots_reset]{plots_reset()}} always has something to go
back to.

Collect plots with \code{\link[=plots_append]{plots_append()}}, take one out with \code{\link[=plots_get]{plots_get()}},
change one with \code{\link[=plots_set]{plots_set()}}, and write them all with \code{\link[=save_plots]{save_plots()}}.
}
\examples{
library(ggplot2)
plots <- plots_init(name = "Brand tracker")
plots <- plots_append(
  plots,
  ggplot(mtcars, aes(wt, mpg)) + geom_point() + labs(title = "Weight vs mileage"),
  name = "Basics/scatter", type = "point", show = FALSE
)
plots

# Wider heatmaps than the default, for every plot in this collection.
plots_init(dims = plots_dims(heatmap = c(18, 11)))

}
\seealso{
\code{\link[=plots_append]{plots_append()}}, \code{\link[=plots_get]{plots_get()}}, \code{\link[=plots_set]{plots_set()}}, \code{\link[=plots_params]{plots_params()}}.
}
