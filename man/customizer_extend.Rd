% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-customizer.R
\name{customizer_extend}
\alias{customizer_extend}
\title{Add parameters to a customizer}
\usage{
customizer_extend(base, apply, params)
}
\arguments{
\item{base}{The customizer to build on, usually \code{\link[=customizer_default]{customizer_default()}}.}

\item{apply}{A function whose first argument is the plot. Its other
arguments are the parameters, and each one must default to \code{NULL}, meaning
"leave this alone". It returns a ggplot.}

\item{params}{A function of the plot, returning a named list of \verb{param_*()}
objects. The names are the argument names of \code{apply}.}
}
\value{
A customizer.
}
\description{
Builds a customizer that offers everything \code{base} offers plus
whatever \code{apply} and \code{params} add. Use it to keep the labels from
\code{\link[=customizer_default]{customizer_default()}} while adding something of your own, rather than
listing them again.

A parameter of the same name replaces the one it shadows.
}
\examples{
library(ggplot2)

# The default labels, plus a switch for the legend's own title.
customizer_extend(
  customizer_default(),
  apply = function(plot, legend_title = NULL) {
    if (is.null(legend_title)) plot else plot + labs(color = legend_title)
  },
  params = function(plot) list(legend_title = param_text("Legend title"))
)

}
