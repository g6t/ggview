% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plots_get}
\alias{plots_get}
\title{Take a plot out of a collection}
\usage{
plots_get(plots, name = NULL, id = NULL)
}
\arguments{
\item{plots}{A collection, from \code{\link[=plots_init]{plots_init()}}.}

\item{name}{Name of the plot. The last plot in the collection is returned
when both \code{name} and \code{id} are omitted.}

\item{id}{Id of the plot, as an alternative to \code{name}. An id stays the same
when a plot is updated, so it is the safer key for code that edits a
stored collection.}
}
\value{
A ggplot object with a \code{\link[=canvas]{canvas()}}.
}
\description{
Returns one plot, ready to render: the stored plot with its
changes applied and its canvas set. Printing the result shows it at the
size it will be saved at.

The changes are applied to the stored plot every time, never to an
already-changed one, so asking twice gives the same plot twice.
}
\examples{
library(ggplot2)
plots <- plots_init()
plots <- plots_append(plots, ggplot(mtcars, aes(wt, mpg)) + geom_point(),
                      name = "scatter", type = "point", show = FALSE)

p <- plots_get(plots, "scatter")
p$canvas$width

}
\seealso{
\code{\link[=as.list]{as.list()}} for every plot at once.
}
