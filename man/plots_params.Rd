% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plots_params}
\alias{plots_params}
\title{What a plot lets people change}
\usage{
plots_params(plots, name = NULL, id = NULL)
}
\arguments{
\item{plots}{A collection, from \code{\link[=plots_init]{plots_init()}}.}

\item{name}{Name of the plot. The last plot in the collection is returned
when both \code{name} and \code{id} are omitted.}

\item{id}{Id of the plot, as an alternative to \code{name}. An id stays the same
when a plot is updated, so it is the safer key for code that edits a
stored collection.}
}
\value{
A list with one entry per parameter, each holding \code{key}, \code{type},
\code{label}, \code{default}, and \code{value} when the parameter has been changed. A
\code{choice} also carries its \code{choices}, a \code{mapping} its \code{keys} and what it
maps \code{to}, and a \code{number} any \code{min}, \code{max} and \code{step}.
}
\description{
Lists one plot's parameters: what each one is called, what kind
of value it takes, what it is by default, and what it has been changed to.
This is what a program reads to offer the right control for each one, and
what \code{\link[=plots_set]{plots_set()}} checks a change against.

The canvas comes last. Every plot accepts \code{width} and \code{height} whatever
its customizer offers.
}
\examples{
library(ggplot2)
plots <- plots_init()
plots <- plots_append(plots, ggplot(mtcars, aes(wt, mpg)) + geom_point(),
                      name = "scatter", type = "point",
                      title = "Weight vs mileage", show = FALSE)

plots_params(plots, "scatter")

}
\seealso{
\code{\link[=customizer]{customizer()}}, \code{\link[=plots_set]{plots_set()}}.
}
